type IMovieCard = {
  genres: string[];
  id: number;
  language: string;
  imageUrl: string;
  releaseYear: string;
  title: string;
  votePercent: string;
};

type IPersonCard = {
  id: number;
  imageUrl: string;
  name: string;
};
